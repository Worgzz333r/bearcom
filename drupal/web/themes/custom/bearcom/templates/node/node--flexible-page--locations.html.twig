{#
/**
 * @file
 * Location Parent Page — Branch Locator & Directory.
 * Flexible page at /locations.
 *
 * Content order managed via paragraphs in admin:
 *   1. Hero Banner (soundwave style)
 *   2. Location Finder (search + map + state directory)
 *   3. CTA Block (optional)
 */
#}

<article{{ attributes.addClass('location-parent') }}>
  {{ content.field_paragraphs }}
</article>
