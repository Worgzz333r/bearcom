{# Guided Journey — tabs component #}
<section class="guided-journey" data-tabs>
  <div class="container">
    {% if paragraph.field_title.value %}
      <div class="section__header">
        <h2 class="section__title">{{ paragraph.field_title.value }}</h2>
        {% if paragraph.field_subtitle.value %}
          <p class="section__subtitle">{{ paragraph.field_subtitle.value }}</p>
        {% endif %}
      </div>
    {% endif %}

    {# Tab navigation #}
    <div class="guided-journey__nav">
      {% for item in paragraph.field_tabs %}
        <button class="guided-journey__tab{% if loop.first %} is-active{% endif %}" data-tab-trigger="{{ loop.index0 }}">
          {{ item.entity.field_tab_title.value }}
        </button>
      {% endfor %}
    </div>

    {# Tab panels #}
    <div class="guided-journey__panels">
      {% for item in paragraph.field_tabs %}
        {% set tab = item.entity %}
        <div class="guided-journey__panel{% if loop.first %} is-active{% endif %}" data-tab-panel="{{ loop.index0 }}">
          <div class="guided-journey__panel-inner">
            <div class="guided-journey__panel-content">
              <span class="guided-journey__caption">Caption leave here</span>

              <h3 class="guided-journey__panel-title">{{ paragraph.field_title.value }}</h3>

              {% if tab.field_body.value %}
                <div class="guided-journey__body">{{ tab.field_body.value|raw }}</div>
              {% endif %}

              {% if tab.field_description.value %}
                <div class="guided-journey__checklist">{{ tab.field_description.value|raw }}</div>
              {% endif %}
            </div>

            {% set media = tab.field_image.entity %}
            {% if media %}
              {% set file_uri = media.field_media_image.entity.uri.value %}
              <div class="guided-journey__panel-image">
                <img src="/sites/default/files/{{ file_uri|replace({'public://': ''}) }}" alt="{{ tab.field_tab_title.value }}">
              </div>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
