{# Stat Item — number + label in horizontal card #}
<div class="stats-counter__item">
  <span class="stats-counter__number" data-stat-number>{{ paragraph.field_number.value }}</span>
  <span class="stats-counter__label">{{ paragraph.field_label.value }}</span>
</div>
