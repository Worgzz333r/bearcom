{#
/**
 * @file
 * View: Industries Listing — /industries
 * Grid 3×3 of industry teasers.
 * Hero and CTA are managed via flexible_page node, not hardcoded here.
 */
#}

<div{{ attributes.addClass('industries-section') }}>
  <div class="industries-grid">
    {{ rows }}
  </div>

  {% if pager %}
    {{ pager }}
  {% endif %}
</div>
